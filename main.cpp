#include <iostream>
#include <stdio.h>
#include <vector>
#include "geneticAlgorithm.h"
using namespace cv;
using namespace std;
int main(int, char**) {
    mainGenetic();
    waitKey(0);
    return 0;
}
